<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projeto_organizze">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projeto_Organizze">
        <activity android:name=".activity.Reservas"></activity>
        <activity android:name=".activity.Agenda" />
        <activity android:name=".activity.Procedimento" />
        <activity android:name=".activity.CadastraUsuario" />
        <activity android:name=".activity.Home" />
        <activity android:name=".activity.Login_" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>